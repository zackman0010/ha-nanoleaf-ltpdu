__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# frontend/ build tooling — the *output* (custom_components/.../www/*.js) is
# committed since HACS users never run npm; the toolchain and its deps are not.
frontend/node_modules/
